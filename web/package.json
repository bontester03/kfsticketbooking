{
  "name": "kfs-web",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.7.0",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "portal": "pnpm --filter portal",
    "admin": "pnpm --filter admin",
    "scanner": "pnpm --filter scanner"
  },
  "devDependencies": {
    "turbo": "^2.0.6",
    "typescript": "^5.5.3"
  }
}
